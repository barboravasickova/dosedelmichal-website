---
import HomeLayout from '../../layouts/HomeLayout.astro';
import { pageTitle } from '../../lib/i18n';

const locale = 'cs';
const currentPath = '/cs/';
---

<HomeLayout
	locale={locale}
	currentPath={currentPath}
	title={pageTitle(locale)}
	description="DOSE — Michal Doseděl. Portfolio malíře a graffiti umělce."
/>
